apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogue-blue
  namespace: roboshop
  labels:
    purpose: blue-green
    project: roboshop
    color: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      purpose: blue-green
      project: roboshop
      color: blue
  template:
    metadata:
      labels:
        purpose: blue-green
        project: roboshop
        color: blue
    spec:
      containers:
      - name: catalogue
        image: robotshop/rs-catalogue:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
