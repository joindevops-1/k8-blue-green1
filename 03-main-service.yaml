apiVersion: v1
kind: Service
metadata:
  name: sample
spec:
  type: LoadBalancer
  selector:
    purpose: blue-green
    project: sample
    color: blue          # LIVE traffic — patch to "green" during cutover
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
