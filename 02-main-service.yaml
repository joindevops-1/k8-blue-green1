apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
spec:
  selector:
    purpose: blue-green
    project: roboshop
    color: blue          # LIVE traffic → change to green during cutover
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
