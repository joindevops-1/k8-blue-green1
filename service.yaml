apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
spec:
  selector:
    app: catalogue
    version: blue    # switch to "green" to cut over traffic
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
